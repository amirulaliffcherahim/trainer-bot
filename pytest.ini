[pytest]
# Make `pytest` (without `python -m`) import project modules from the root.
pythonpath = .
# Collect async tests without requiring per-test markers (plugin needed:
# pip install -r requirements-dev.txt).
asyncio_mode = auto
